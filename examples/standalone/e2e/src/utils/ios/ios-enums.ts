export enum XCUIElements {
  Button = 'XCUIElementTypeButton',
  StaticText = 'XCUIElementTypeStaticText',
}

export enum PredicateStringComparators {
  BeginsWith = 'BEGINSWITH',
  Contains = 'CONTAINS',
  EndsWith = 'ENDSWITH',
  Matches = 'MATCHES',
}
